# 在线医疗咨询系统 - 项目进度报告

## 一、已完成的功能

### 1. 后端基础架构
- [x] Spring Boot 3.2.6 + WebSocket (STOMP协议)
- [x] Redis 配置（db2 用于队列和医生状态）
- [x] MySQL 数据库（MyBatis-Plus）
- [x] CORS 跨域配置
- [x] WebSocketConfig 配置

### 2. 医生端
- [x] 医生登录 (`/doctor/login`)
- [x] 医生下线 (`/doctor/logout`)
- [x] 医生心跳保活 (`/doctor/heartbeat`)
- [x] 获取在线医生列表 (`/doctor/list`)
- [x] 医生工作台 WebSocket 连接
- [x] 订阅排队患者列表更新 (`/topic/doctor.queue.{doctorId}`)
- [x] 医生叫号、发送消息、结束问诊

### 3. 患者端
- [x] 患者登录 (`/user/login`)
- [x] 创建问诊会话 (`/consult/create`)
- [x] 医生列表展示
- [x] 排队页面
- [x] 订阅排队位置更新 (`/topic/queue.{patientId}`)
- [x] 被叫到通知 (`/topic/queue.{patientId}.called`)
- [x] 患者聊天页面

### 4. 队列系统
- [x] 患者加入排队 (`joinQueue`)
- [x] 患者取消排队 (`cancelQueue`)
- [x] 医生叫号 (`callPatient`)
- [x] 计算排队位置和预计等待时间
- [x] 广播排队患者列表给医生 (`broadcastQueueList`)

### 5. Bug 修复
- [x] 修复医生登录后状态显示"离线" - 改为 `doctorInfo.doctor.status`
- [x] 修复刷新页面登录状态丢失 - 添加 `initFromStorage()`
- [x] 修复 `heartbeat` 和 `logout` 使用错误的 doctorId
- [x] 移除医生工作台假数据
- [x] 修复 `joinQueue` 未保存 `sessionId`
- [x] 将 `joinQueue` 移到 WebSocket 连接建立后调用

---

## 二、待解决的问题

### 1. 患者排队位置一直显示"计算中..."
**现象**：患者进入排队页面后，位置一直显示"计算中..."，预计等待0分钟

**可能原因**：
- WebSocket 消息未正确发送到患者端
- 前端订阅的 destination 与后端发送的不匹配
- `getQueuePosition` 查询返回 null

**待检查**：
- [ ] 后端 `broadcastQueueList` 是否被调用
- [ ] 前端是否正确订阅 `/topic/queue.{patientId}`
- [ ] `getQueuePosition` 查询条件是否正确

### 2. 医生端看不到排队患者
**现象**：患者已排队，但医生工作台的"排队患者"栏为空

**可能原因**：
- 后端 `broadcastQueueList` 未被正确调用
- 医生订阅的 `/topic/doctor.queue.{doctorId}` 与后端发送的不匹配

**待检查**：
- [ ] 后端重启后 `broadcastQueueList` 代码是否生效
- [ ] 后端日志是否有 `=== joinQueue called:` 输出

### 3. WebSocket 频繁断连
**现象**：WebSocket 一直断开重连

**可能原因**：
- 后端不稳定
- 心跳发送了无效的 doctorId (undefined)

**已修复**：
- [x] heartbeat 和 logout 已修复为使用 `doctorInfo.doctor.id`

---

## 三、数据结构

### 登录返回数据
```json
{
  "doctorUser": { "id": 2, "doctorId": 2, "username": "doctor2", ... },
  "doctor": { "id": 2, "name": "李医生", "status": "ONLINE", ... }
}
```

### 访问路径
- 前端地址：`http://localhost:5173`
- 后端地址：`http://localhost:8080`
- WebSocket 端点：`http://localhost:8080/ws`

### 测试账号
- 医生：doctor1, doctor2, doctor3 / 密码 123456
- 患者：patient1, patient2 / 密码 123456

---

## 四、关键文件路径

### 后端
```
D:\xiaozhi-medical\java-ai-langchain4j\
├── src\main\java\com\atguigu\java\ai\langchain4j\
│   ├── controller\
│   │   ├── DoctorController.java
│   │   ├── ConsultController.java
│   │   └── WebSocketController.java
│   ├── service\impl\
│   │   ├── DoctorServiceImpl.java
│   │   ├── QueueServiceImpl.java
│   │   └── ConsultServiceImpl.java
│   ├── config\
│   │   └── WebSocketConfig.java
│   └── dto\
│       └── QueueDTO.java
```

### 前端
```
D:\xiaozhi-medical\xiaozhi-ui\xiaozhi-ui\
├── src\
│   ├── views\doctor\
│   │   ├── DoctorDashboard.vue
│   │   └── DoctorLogin.vue
│   ├── views\patient\
│   │   ├── QueuePage.vue
│   │   ├── DoctorList.vue
│   │   └── PatientChat.vue
│   ├── stores\
│   │   ├── doctor.js
│   │   └── consult.js
│   └── services\
│       └── websocket.js
```

---

## 五、重启后端命令

```bash
cd D:\xiaozhi-medical\java-ai-langchain4j
mvn spring-boot:run
```

---

## 六、下一步排查步骤

1. **重启后端**，确保代码更新生效
2. **医生登录**医生工作台，确认状态显示"在线"
3. **患者登录**后选择医生排队
4. **查看后端控制台**是否有 `=== joinQueue called:` 日志
5. **查看浏览器控制台**患者端是否有 `Queue position received:` 日志
6. 如果没有日志，说明 WebSocket 消息未到达后端或前端

---
